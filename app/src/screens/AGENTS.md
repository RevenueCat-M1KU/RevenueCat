# Screen design

When creating or changing a screen in this directory, read
`docs/DESIGN.md` from the repository root. Apply its rules that do not bend,
the screen's section, and the matching tokens and checks.
